//an object named myProject is defined below

let myProject = {
    title: 'Project',
    numberOfPages: 20,
    projectDescription: 'write briefly and in an understandable way',
}

//don't forget to put comma(,) at the end of the line in an object and colon(:) in between

console.log(`This ${myProject.title} has ${myProject.numberOfPages} pages and the description is ${myProject.projectDescription}`);

